@model IEnumerable<TotalNetCore.DDDGuestbook.Core.Entities.ToDoItem>

@{
    ViewData["Title"] = "ToDo List";
}

<h2>To Do Items (MVC View)</h2>
<ul>
    @foreach (var item in Model)
    {
        <li>@item.Title<br />@item.Description</li>
    }
</ul>
